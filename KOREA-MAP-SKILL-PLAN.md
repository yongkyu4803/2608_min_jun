# korea-map 스킬 계획

한국 광역행정구역(시도) 지도를 프로젝트에 심는 범용 Claude Code 스킬.

- **작성일**: 2026-08-03
- **상태**: 계획 확정, 구현 대기
- **근거**: `2608_min_jundae` 대시보드에 지도 뷰를 구현하며 겪은 실제 문제들을 자산화

---

## 1. 범위

| 항목 | 결정 |
|---|---|
| 행정 단위 | **광역(시도) 17개만**. 시군구·읍면동 미지원 |
| 표현 방식 | **경계 지도 + 카토그램 2종** (전환 가능) |
| 적용 대상 | **범용** — 특정 프로젝트의 스타일 체계에 묶이지 않음 |
| 런타임 의존성 | **없음**. 지도 라이브러리(Leaflet·카카오·react-simple-maps) 불사용 |

### 핵심 설계 판단

경계 데이터를 **빌드 시점에 인라인 SVG 패스로 구워 넣는다.** 결과적으로:

- API 키·도메인 등록 불필요
- 호스트 프로젝트의 테마·다크모드·이미지 내보내기가 그대로 적용됨
- 번들에 63KB 추가로 끝남

광역만 다루므로 데이터가 작다. **경계와 카토그램 좌표를 모두 미리 구워 `assets/`에 넣으면, 스킬 실행 시 다운로드도 변환도 필요 없다.** 파일 복사와 컴포넌트 생성만 하면 된다. 변환 스크립트는 경계가 개편될 때만 쓰는 유지보수 도구로 남긴다.

---

## 2. 산출물 구조

```
korea-map/
├── SKILL.md                  # 워크플로 · 질문 흐름 · 검증 체크리스트
├── assets/
│   ├── sido-geo.json         # 시도 17개 SVG 패스 (63KB)
│   └── sido-cartogram.json   # 시도 17개 카토그램 좌표 + 타일 배치
├── scripts/
│   ├── build-geo.js          # GeoJSON → SVG 패스 (경계 개편 시에만)
│   └── build-cartogram.js    # 지리 중심점 → Dorling 배치 계산
└── references/
    ├── sources.md            # 출처·라이선스
    ├── name-mapping.md       # 정식명↔축약명, 개편 이력
    ├── pitfalls.md           # 함정 9종
    └── design.md             # 색·라벨·접근성 규칙
```

---

## 3. 데이터 출처

| 출처 | 형식 | 기준 | 라이선스 | 채택 |
|---|---|---|---|---|
| [southkorea/southkorea-maps](https://github.com/southkorea/southkorea-maps) (kostat) | GeoJSON·TopoJSON·SVG | 2013 | **Free to share or remix** | **채택** |
| [cubensys/Korea_District](https://github.com/cubensys/Korea_District) | GeoJSON·TopoJSON | 2018 | 명시 없음 | 보류 |
| 통계청 SGIS / 국가공간정보포털 | SHP | 최신 | 공공누리 | 대안 |

**채택 근거**: 라이선스가 명확하고 널리 쓰인다. 2013년 데이터지만 **시도 경계는 2012년 세종시 출범 이후 사실상 변동이 없다.**

> **주의**: southkorea-maps는 저장소 안에서 출처별로 라이선스가 다르다. `gadm/` 는 **비상업적 용도만, 재배포 금지**이므로 절대 쓰면 안 된다. `kostat/` 만 사용한다.

파일 경로:
```
kostat/2013/json/skorea_provinces_geo.json   # 27MB, WGS84(EPSG:4326)
```

---

## 4. 카토그램 설계

두 방식을 제공하고 기본값을 정한다.

| 방식 | 배치 | 크기 | 적합한 상황 |
|---|---|---|---|
| **타일 그리드** (기본) | 17칸 고정, 지리 배열 모방 | 모두 동일 | 라벨이 항상 읽혀야 할 때. 값은 색으로만 표현 |
| **Dorling** | 지리 중심점에서 출발해 충돌 해소 | 가중치 비례 | "표의 무게"를 크기로 보여줄 때 |

**기본은 타일 그리드를 권한다.** 배치가 고정이라 데이터가 바뀌어도 화면이 출렁이지 않고, 17칸 모두 라벨이 들어간다. Dorling은 크기가 정직한 대신 매번 배치가 달라져 시점 간 비교가 어렵다.

**타일 배치 좌표는 스킬에 고정값으로 박는다.** 자동 생성하면 결과가 어색해지고, 17개는 손으로 잡는 편이 훨씬 낫다. 구현 시작 시 배치 초안을 먼저 확인받는다.

---

## 5. 범용 대응

호스트 프로젝트를 모른다는 점이 범용 스킬의 실질 비용이다. 세 가지로 흡수한다.

### 5.1 색을 주입받는다

컴포넌트가 색을 하드코딩하지 않고 `colors` prop 또는 CSS 변수명을 받는다. 프로젝트에 디자인 토큰이 있으면 그것을 쓰고, 없으면 스킬이 검증된 기본 램프를 제공한다.

### 5.2 스택을 감지한다

`package.json` 으로 React·Tailwind 유무를 판별해 분기한다. React가 없으면 순수 SVG 문자열 생성으로 폴백한다.

### 5.3 데이터 계약을 하나로 고정한다

```ts
type MapDatum = {
  region: string;   // 축약명 기준 ("충남")
  value: number;    // 색 농도를 정하는 값
  weight?: number;  // Dorling 카토그램의 크기 (선거인수, 인구 등)
  label?: string;   // 보조 설명
};
```

`region` 정규화 함수를 함께 제공해 정식명(`충청남도`)·영문명(`Chungcheongnam-do`)·행정코드(`44`)를 모두 받아준다.

---

## 6. 스킬 실행 흐름

1. **행정 단위 확인** — 광역만 지원함을 명시. 시군구 요청 시 미지원을 분명히 밝히고 대안 안내
2. **표현 선택** — 경계 지도 / 카토그램 / 둘 다 전환
3. **지표 확인** — 값의 성격(연속·순위·범주)에 따라 색 규칙 결정
4. **스택 감지 후 생성** — 데이터 파일 + 컴포넌트
5. **검증** — 아래 체크리스트 수행

---

## 7. references/pitfalls.md — 실제로 걸린 함정 9종

이 스킬의 핵심 가치. 모두 구현 과정에서 실제 발생한 문제다.

1. **닫힌 링에서 RDP 단순화가 붕괴한다**
   시작점=끝점이라 기준선 길이가 0이 되고 모든 점의 거리가 0으로 계산되어 폴리곤이 2점으로 뭉개진다. **17개 시도가 전부 사라졌다.**
   → 시작점에서 가장 먼 점을 기준으로 링을 둘로 갈라 각각 RDP를 돌린 뒤 다시 잇는다.

2. **원본 27MB에서 재귀 스택이 넘친다**
   → `node --stack-size=8000` 필요.

3. **같은 저장소 안에서 라이선스가 다르다**
   southkorea-maps의 KOSTAT은 자유롭지만 GADM은 재배포 금지. 폴더를 잘못 고르면 라이선스 위반.

4. **viewBox에 여백이 남는다**
   고정 크기로 잡지 말고 경계 비율로 높이를 계산한다.

5. **소면적 시도는 라벨이 겹친다**
   세종·대전, 부산·울산이 뭉개진다. → 오프셋 + 지시선(leader line).

6. **이름이 안 맞는다**
   `충청남도`↔`충남`, `강원도`→`강원특별자치도`(2023), `전라북도`→`전북특별자치도`(2024). 매핑 테이블 필수.

7. **면적이 값을 왜곡한다**
   경북·강원은 넓지만 표는 적다. 지도를 막대차트의 대체가 아니라 보조로 두고 안내 문구를 넣는다. (카토그램이 이 문제의 정공법)

8. **무데이터와 0을 구분해야 한다**
   미개표 지역을 0으로 칠하면 거짓말이 된다. → 별도 회색 + 범례.

9. **값을 호버로만 읽게 하면 안 된다**
   → 옆에 목록을 두어 접근성을 확보한다.

---

## 8. 검증 체크리스트

SKILL.md 에 명시하고 매 실행 후 수행한다.

- [ ] 17개 시도가 모두 렌더되는가 (RDP 붕괴 사고 방지)
- [ ] 라벨이 겹치지 않는가 (세종·대전, 부산·울산)
- [ ] 무데이터 지역이 0과 구분되는가
- [ ] 값을 호버 없이 읽을 수 있는가
- [ ] 팔레트가 라이트/다크 모두 통과하는가
- [ ] 면적 왜곡 경고 문구가 있는가 (경계 지도일 때)

---

## 9. 작업량

**약 1일.**

- 경계 데이터·변환 스크립트: 재사용 (이미 존재)
- 카토그램 좌표: 손으로 배치 + 확인
- 범용화 레이어: **가장 손이 많이 가는 부분.** 현재 컴포넌트는 `--viz-*` 토큰에 묶여 있어 주입식으로 푸는 작업이 필요
- 문서(references 4종)

### 구현 순서

1. `SKILL.md` 초안
2. 카토그램 타일 배치 (초안 확인 후 확정)
3. 범용 컴포넌트 (색 주입 · 스택 감지)
4. references 문서
5. 빈 프로젝트에서 실행 검증

---

## 10. 참고 — 원본 구현

이 계획의 근거가 된 실제 구현은 `2608_min_jundae` 프로젝트에 있다.

| 파일 | 내용 |
|---|---|
| `scripts/build-korea-map.mjs` | GeoJSON → SVG 패스 변환기 (RDP 링 분할 포함) |
| `src/lib/korea-map.ts` | 생성된 시도 17개 패스 (63KB) |
| `src/components/dashboard/korea-map.tsx` | 단계구분도 컴포넌트 (라벨 오프셋·순위 목록·범례) |
